.user
  = link_to user.email, user
  = user.uid
  Given: #{user.tips_given.sum(:amount)}
  Received: #{user.tips_received.sum(:amount)}
  = render partial: 'tips/form', locals: {tip: Tip.new(tipper: current_user, tippee: user)} if user != current_user
